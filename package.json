{
  "name": "aci",
  "version": "0.1.0",
  "description": "A deterministic response model: signals move a brain between states, and it expresses the state it is in.",
  "type": "module",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "exports": {
    ".": "./dist/index.js",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "esdev test",
    "build": "esdev build --lib src --out=dist",
    "dev": "esdev start --config=demo/esdev.json",
    "build:site": "esdev build --config=demo/esdev.json --minify"
  },
  "devDependencies": {
    "@opentf/esrun-types": "latest"
  },
  "dependencies": {
    "@opentf/std": "^0.18.0"
  }
}
