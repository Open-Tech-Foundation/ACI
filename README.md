# ACI

A deterministic problem-solving system. Not a general-purpose AI model.

Rebuilding from scratch. Nothing is implemented yet, and nothing is designed
yet — the previous engine and its specification were removed rather than
adapted.

## Running it

Everything goes through [`tsr`](https://tsr.opentechf.org/), from the
repository root. See `tasks.toml`.
